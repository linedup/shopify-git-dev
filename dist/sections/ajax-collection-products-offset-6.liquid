<div id="ajax-collection-products">

  <div class="max-w-screen-2xl mx-auto">
    <div class="grid-container page-margin mt-9 lg:mt-18__ mb-9 lg:mb-18"> <!-- To do: Spacing a little uncertain -->
      {% comment %} Offset is set to allow for the 6 products shown on the landing page {% endcomment %}
      {%- for product in collection.products offset: 6 -%}
        {% render 'collection-regular-product', product: product, showVideo: showVideo %}
      {%- endfor -%}
    </div>
  </div>

</div>