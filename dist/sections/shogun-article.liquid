<div class="max-w-screen-2xl mx-auto mb-14 lg:mb-18">
  <div class="grid-container page-margin overflow-hidden">
    <div class="col-span-4 md:col-span-9 lg:col-span-8 xl:col-span-7">
      <h1>{{ article.title }}</h1>
    </div>
  </div>
</div>

<div class="max-w-screen-2xl mx-auto mb-14 lg:mb-18">
  <div class="grid-container page-margin">
    <div class="col-span-12 mb-5">
      {% comment %} Removing the Shogun script which adds jQuery and CSS to the DOM {% endcomment %}
      {% comment %} Removing the Shogun overlay div in order to access the first-child in the dom structure {% endcomment %}
      {{ article.content 
        | replace: '<script type="text/javascript" src="https://cdn.getshogun.com', '<script type="text/javascript" src="'
        | replace: '<div class="shg-box-overlay"></div>', ''
      }}
    </div>
  </div>
</div>

{% render 'share' %}