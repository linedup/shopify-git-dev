    <!-- Account screen -->
    <div class="col-span-4 md:col-span-12 lg:col-span-3 mt-10">
      <div class="grid grid-cols-1 gap-y-5 lg:gap-y-10">
        <div class="col-span-1">
          <h3>Customer Support</h3>
        </div>
        <div class="col-span-4 md:col-span-12">
          <ul>
            {% for link in linklists.customer-support.links %}
              <li {% if forloop.last == false %}class="mb-2"{% endif %}><a href="{{ link.url }}">{{ link.title }}</a></li>
            {% endfor %}
          </ul>
        </div>
        <div class="col-span-4 md:col-span-12 flex justify-center mb-8 mt-4 lg:hidden">
          <hr class="w-full h-px inline-block bg-richBlack">
        </div>
      </div>
    </div>